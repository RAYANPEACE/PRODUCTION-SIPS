{
  "name": "production-sips",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "server": "node server/app.mjs",
    "check:js": "node -e \"const fs=require('fs'),vm=require('vm');const h=fs.readFileSync('index.html','utf8');const m=h.match(/<script>([\\s\\S]*?)<\\/script>/);vm.createScript(m[1]);console.log('JS OK')\""
  }
}
