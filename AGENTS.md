# Codex Guidance

## Default style

- Prefer the smallest correct solution that actually works.
- Reuse existing project patterns and native/standard tools before adding dependencies.
- Keep changes tightly scoped to the requested bug or feature.

## Windows shell

- In PowerShell, use `npm.cmd` instead of `npm`; this machine blocks `npm.ps1` by execution policy.
